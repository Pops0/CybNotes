**Metasploit Framework** is huge and installs tons of dependencies.

Docker avoids breaking your system.

Run it:

```
docker run --rm -it metasploitframework/metasploit-framework
```

You instantly get:

msfconsole

Great for:

- exploit testing
    
- payload generation
    
- exploit research